import { useNavigate } from 'react-router';
const sections = [["Project questions", "Use GitHub issues for setup questions, bug reports and ideas. Include the part you are using and what you expected. Use sample data; never post credentials or client records."], ["Contributions", "Read CONTRIBUTING.md before proposing a change. Small, focused improvements are welcome."], ["A project of your own", "For a conversation with the studio, visit MGLO Software. For sensitive security reports, follow SECURITY.md in the repository."]];
export default function Contact() {
 const navigate=useNavigate();
 return <main className="min-h-screen bg-bg-primary px-6 py-16 text-text-primary"><div className="max-w-3xl mx-auto"><button className="text-accent-secondary mb-12" onClick={()=>navigate('/')}>← BodyOS</button><h1 className="font-display text-4xl mb-12">Talk about BodyOS</h1>{sections.map(([heading,text])=><section key={heading} className="mb-10"><h2 className="font-display text-xl mb-3">{heading}</h2><p className="text-text-secondary leading-relaxed">{text}</p></section>)}<div className="flex flex-wrap gap-6 text-accent-secondary"><a href="https://github.com/LaoWater/bodyos-open" target="_blank" rel="noreferrer">BodyOS repository ↗</a><a href="https://mglo-software.com" target="_blank" rel="noreferrer">MGLO Software ↗</a></div></div></main>;
}
