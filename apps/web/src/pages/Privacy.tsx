import { useNavigate } from 'react-router';
const sections = [["The demo workspace", "The demo uses sample records. It saves interface preferences and demo mode in your browser. Clear this site’s browser storage to remove those local settings. Do not put personal client information into a public demo."], ["Connected installations", "Accounts and provider connections are optional and configured by the installation owner. Their data practices depend on that configuration. Before collecting client data, that operator must provide their own privacy information and retention arrangements."], ["Camera and coaching", "The native iOS pose path processes camera frames on the device. Mobile training history uses local storage. The optional AI coaching function sends conversation content and supplied context to its configured AI provider. These are distinct paths; do not assume every feature stays on-device."]];
export default function Privacy() {
 const navigate=useNavigate();
 return <main className="min-h-screen bg-bg-primary px-6 py-16 text-text-primary"><div className="max-w-3xl mx-auto"><button className="text-accent-secondary mb-12" onClick={()=>navigate('/')}>← BodyOS</button><h1 className="font-display text-4xl mb-12">Privacy and the community demo</h1>{sections.map(([heading,text])=><section key={heading} className="mb-10"><h2 className="font-display text-xl mb-3">{heading}</h2><p className="text-text-secondary leading-relaxed">{text}</p></section>)}<div className="flex flex-wrap gap-6 text-accent-secondary"><a href="https://github.com/LaoWater/bodyos-open" target="_blank" rel="noreferrer">BodyOS repository ↗</a><a href="https://mglo-software.com" target="_blank" rel="noreferrer">MGLO Software ↗</a></div></div></main>;
}
