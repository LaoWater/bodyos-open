import { useNavigate } from 'react-router';
const sections = [["Why we are sharing it", "MGLO Software built BodyOS across web, mobile, native computer vision, data and machine learning research. We are sharing that foundation so people who care about training can adapt it to their own practice."], ["Where to begin", "Explore the sample workspace, read the short setup guide, then choose one part to customize. The repository includes instructions for people working by hand or with a coding assistant."], ["Work with us", "Contributions are welcome: a useful fix, a clearer guide, exercise knowledge or feedback from real practice. Read the contribution guide on GitHub."]];
export default function About() {
 const navigate=useNavigate();
 return <main className="min-h-screen bg-bg-primary px-6 py-16 text-text-primary"><div className="max-w-3xl mx-auto"><button className="text-accent-secondary mb-12" onClick={()=>navigate('/')}>← BodyOS</button><h1 className="font-display text-4xl mb-12">A gift to the fitness community</h1>{sections.map(([heading,text])=><section key={heading} className="mb-10"><h2 className="font-display text-xl mb-3">{heading}</h2><p className="text-text-secondary leading-relaxed">{text}</p></section>)}<div className="flex flex-wrap gap-6 text-accent-secondary"><a href="https://github.com/LaoWater/bodyos-open" target="_blank" rel="noreferrer">BodyOS repository ↗</a><a href="https://mglo-software.com" target="_blank" rel="noreferrer">MGLO Software ↗</a></div></div></main>;
}
